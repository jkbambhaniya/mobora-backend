# Server Config
PORT=5000

# Database Config
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mobora

# Authentication (JWT)
JWT_SECRET=super_secret_jwt_key_please_change_in_production
JWT_EXPIRES_IN=1d

# Developer Options
# Set to true to automatically approve newly registered vendors (no admin activation needed).
# Set to false to default vendors to 'pending' state.
AUTO_APPROVE_VENDORS=false
